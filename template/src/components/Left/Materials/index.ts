import commonTitleCmps from "./title";

const keysToCmps = {
    "title": commonTitleCmps
}

export type Keys = "title" | "img"
export const keys: Keys[] = ['title', "img"]

export default keysToCmps;
